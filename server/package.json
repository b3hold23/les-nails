{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "type":"module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "render-build": "npm install && npx tsc",
    "build": "npx tsc",
    "start": "node dist/server.js",
    "seed": "node dist/seeds/index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/express": "^5.0.0",
    "express": "^4.21.2",
    "mongoose": "^8.9.3"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
